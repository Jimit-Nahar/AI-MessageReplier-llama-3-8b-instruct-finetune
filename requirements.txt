# Core LLM libraries
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.22.0
peft>=0.8.2
trl>=0.7.10
bitsandbytes>=0.42.0

# Unsloth (from GitHub, not PyPI)
git+https://github.com/unslothai/unsloth.git#egg=unsloth[colab-new]

# FlashAttention – install separately with no-deps (see note below)
flash-attn

# Utility libraries
packaging>=23.0
ninja>=1.11.1
einops>=0.7.0

# OCR and image processing
pytesseract>=0.3.10
Pillow>=9.5.0
opencv-python>=4.8.0
numpy>=1.23.0

# Tesseract OCR system binary (note: not installable via pip)
# ➤ Must be installed via apt on Linux or Brew on macOS
# sudo apt install tesseract-ocr
